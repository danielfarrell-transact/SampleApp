name: Test

on:
  push:
    branches:
     - master
  pull_request:
    branches:
     - master

jobs:

  Build:
    runs-on: macOS-latest
    steps:
      - name: Run Unit Tests
        run: xcodebuild build -workspace SampleApp.xcworkspace -scheme SampleApp
